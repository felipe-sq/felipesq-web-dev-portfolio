node_modules
.next
out
.DS_Store
assets/.DS_Store
.env*.local
.vercel

# Per-machine Claude Code overrides (absolute paths, personal permission grants).
# Shared config belongs in .claude/settings.json, which is not ignored.
.claude/settings.local.json
