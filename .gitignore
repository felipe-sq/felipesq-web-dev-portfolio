node_modules
.next
out
.DS_Store
assets/.DS_Store
.env*.local
.vercel

# Editor and agent config is kept personal, not shared. Covers Claude Code's
# settings.local.json (absolute paths, personal permission grants) and
# launch.json, and any VS Code workspace settings.
.claude/
.vscode/

# Agent tooling artifacts, regenerated on demand and per-machine.
.agents/
skills-lock.json
