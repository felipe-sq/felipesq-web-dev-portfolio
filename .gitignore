node_modules
.next
out
.DS_Store
assets/.DS_Store
.env*.local
.vercel

# Claude Code config is kept personal, not shared. Covers settings.local.json
# (absolute paths, personal permission grants) and launch.json.
.claude/

# Agent tooling artifacts, regenerated on demand and per-machine.
.agents/
skills-lock.json
